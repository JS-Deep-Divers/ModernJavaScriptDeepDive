(Chap 8)
### **1. break와 continue의 차이**

break 문은 코드 블록을 탈출한다.

continue 문은 반복문의 코드 블록 실행을 현 지점에서 중단하고 반복문의 증감식으로 실행 흐름을 이동시킨다. break 문처럼 반복문을 탈출하지는 않는다.

(Chap 9)

### **2. 명시적 타입 변환 중 숫자 타입으로 변환하는 방법 중 하나로, 사용하는 2가지 함수는? (문자열만 숫자 타입으로 변환 가능함)**

parseInt, parseFloat

### **3. null 병합 연산자 ??는 좌항의 피연산자가 Falsy값인 경우 **의 피연산자를 반환한다. 
여기서 **에 들어갈 말은?**

“좌항”
Falsy 값(false, undefined, null, 0, -0. NaN. ’ ’)이라도 null 또는 undefined가 아니면 좌항의 피연산자를 그대로 반환한다.
